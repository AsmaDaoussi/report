% ============================================================
% CHAPTER 5 -- AI SERVICES, VR IMMERSIVE PREVIEW AND DEPLOYMENT
% ============================================================
\chapter{Advanced Features}
\label{chap:sprint5}
\chaptersommaire
\clearpage

\chapterintro{%
This chapter presents the advanced capabilities that distinguish the IGS Marketplace from a conventional asset store: AI-powered services integrated into the user experience, security hardening mechanisms protecting the platform against common vulnerabilities, and an immersive WebXR preview feature complemented by a production deployment infrastructure.%
}

% ---- Colored concept-box styles used throughout this chapter ----
\definecolor{aiBlue}{RGB}{31,78,121}
\definecolor{aiGreen}{RGB}{0,130,80}
\definecolor{aiPurple}{RGB}{100,30,140}
\definecolor{aiOrange}{RGB}{180,80,0}
\definecolor{aiRed}{RGB}{180,20,20}

% Generic concept box: \conceptbox{color}{Title}{body}
\newtcolorbox{conceptbox}[2][aiBlue]{
  enhanced,
  colback=#1!4,
  colframe=#1,
  leftrule=4pt,
  rightrule=0.3pt,
  toprule=0.3pt,
  bottomrule=0.3pt,
  arc=3pt,
  top=6pt, bottom=6pt, left=10pt, right=8pt,
  fonttitle=\bfseries\color{white},
  coltitle=white,
  title=#2,
  attach boxed title to top left={yshift=-2mm, xshift=8mm},
  boxed title style={colback=#1, colframe=#1, arc=2pt, top=2pt, bottom=2pt, left=6pt, right=6pt},
}

% ============================================================
% AI SERVICES
% ============================================================

\section{AI Services}

Three AI services enrich the platform with intelligent behaviour. Each was selected to match the available infrastructure and the nature of the data: a large language model for conversational assistance, a pre-trained transformer for sentiment classification, and a deterministic similarity algorithm for personalized recommendations.

% ── 5.2.1 AI CHATBOT ─────────────────────────────────────────────────────────
The first AI service is a conversational assistant integrated into every page of the marketplace.

\subsection{AI Chatbot}

\subsubsection{Analysis}

\paragraph{Overview.}
The chatbot is a conversational assistant integrated into every page of the marketplace. It allows users to search the catalog, ask platform-related questions and retrieve order information in natural language. The system operates through a three-stage pipeline: intent classification, optional catalog data retrieval, and response generation. This architecture keeps latency low by routing simple requests locally and only invoking the language model when a free-form answer is required.

\paragraph{Stage 1 --- Intent Classification with TF-IDF and Linear SVM.}
Intent classification determines the purpose of a user message before any further processing. The platform uses a two-step approach: statistical vectorization followed by a linear classifier.

\vspace{4pt}
\begin{conceptbox}[aiBlue]{TF-IDF --- Term Frequency--Inverse Document Frequency}
\textbf{TF-IDF}~\cite{tfidf} is a statistical measure that quantifies the importance of each word in a message relative to the entire training corpus. Words that appear frequently in the message but rarely across all messages receive high scores, making them strong discriminators. Formally, for a word $w$ in document $d$ from corpus $D$:
\[
\text{TF-IDF}(w, d, D) \;=\; \frac{f_{w,d}}{\displaystyle\sum_{w' \in d} f_{w',d}} \;\times\; \log\!\left(\frac{|D|}{|\{d' \in D : w \in d'\}|}\right)
\]
Each message is transformed into a sparse numerical vector representing its lexical content.
\end{conceptbox}

\vspace{4pt}
\begin{conceptbox}[aiGreen]{Support Vector Machine}
A \textbf{Support Vector Machine}~\cite{svm} then classifies the TF-IDF vector. SVMs find the optimal separating hyperplane between training classes by maximizing the margin between the closest examples of each class (the \textit{support vectors}). The linear variant is particularly effective for text classification tasks, since TF-IDF feature spaces are generally linearly separable and the linear kernel trains efficiently on high-dimensional sparse data~\cite{sklearn}.
\end{conceptbox}

The classifier recognizes six intent categories, illustrated in Table~\ref{tab:intent_cats}.

\begin{table}[H]
\centering
\caption{Intent categories handled by the classifier}
\label{tab:intent_cats}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|L{4cm}|L{9.3cm}|}
\hline
\rowcolor{isiBlue}
\textcolor{white}{\textbf{Intent}} & \textcolor{white}{\textbf{Example utterance}} \\
\hline
Product search     & ``Show me animated character models under \$10'' \\
\hline
Platform help      & ``How do I upload my first asset?'' \\
\hline
Order status       & ``Where is my download?'' \\
\hline
Seller information & ``How do commissions work?'' \\
\hline
Greeting           & ``Hello, what can you help me with?'' \\
\hline
Out of scope       & ``What is the weather today?'' \\
\hline
\end{tabular}
\end{table}

\paragraph{Stage 2 --- Retrieval-Augmented Generation.}
When a product-search intent is detected, the platform queries the asset catalog using keywords from the message and injects the matching results into the model's context window. This \textbf{retrieval-augmented generation} (RAG) pattern~\cite{rag} grounds the model's response in real catalog data and eliminates the risk of the model fabricating non-existent products.

\paragraph{Stage 3 --- Response Generation with a Large Language Model.}
The final response is generated by \textbf{Qwen2.5-72B-Instruct}~\cite{qwen25}, a large language model developed by the Qwen Team at Alibaba Cloud.

\vspace{4pt}
\begin{conceptbox}[aiPurple]{Transformer Architecture and Large Language Models}
Large language models are built on the \textbf{Transformer} architecture~\cite{transformer}, which relies on a \textit{self-attention mechanism} allowing every token in the input sequence to attend to every other token simultaneously, capturing long-range dependencies without recurrence. Pre-trained on massive text corpora and subsequently fine-tuned with instruction-following data, these models generate fluent, contextually coherent answers conditioned on a provided system prompt and conversation history.
\end{conceptbox}

The model is served through the Hugging Face Inference API~\cite{huggingface}, eliminating the need for local GPU infrastructure. A rule-based fallback is activated whenever the external service is unreachable, ensuring the assistant remains functional at all times.

\begin{figure}[H]
\centering
\includegraphics[width=0.62\textwidth]{images/description/ai chatbot.png}
\caption{AI Chatbot Pipeline --- intent classification, RAG tool layer, and LLM generation}
\label{fig:chatbot_pipeline}
\end{figure}

\subsubsection{Realization}

The chatbot is available as a floating widget on every page of the marketplace, accessible to both authenticated users and guests. A submitted message passes through the three-stage pipeline and the generated answer is returned to the interface in real time.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{images/realisation/marketplace/chatbot.png}
\caption{AI chatbot widget --- intent-aware assistant available on every page}
\label{fig:chatbot_interface}
\end{figure}

\subsubsection{Conclusion}
The combination of a local intent classifier and a cloud language model achieves both responsiveness and generative quality. Straightforward intents are handled immediately, while complex natural-language queries benefit from the reasoning capacity of the large model.

% ── 5.2.2 REVIEW SENTIMENT ANALYSIS ──────────────────────────────────────────
The second AI service classifies user reviews automatically to support moderators and sellers in interpreting large volumes of feedback.

\subsection{Review Sentiment Analysis}

\subsubsection{Analysis}

\paragraph{Overview.}
Every review submitted by a buyer is automatically analyzed to determine whether its content is positive, neutral or negative. This sentiment classification supports moderators in auditing large volumes of user feedback and provides sellers with an aggregated view of how their assets are perceived, without requiring manual reading of each entry.

\paragraph{Primary Model --- DistilBERT.}
The primary classification engine is \textbf{DistilBERT}~\cite{distilbert}, a compressed variant of the \textbf{BERT} architecture.

\vspace{4pt}
\begin{conceptbox}[aiBlue]{BERT --- Bidirectional Encoder Representations from Transformers}
\textbf{BERT}~\cite{bert} is a pre-trained deep bidirectional Transformer that reads the entire input sequence simultaneously, capturing context from both sides of each word. This bidirectional attention mechanism makes it significantly more effective at understanding negation and nuance than models that process text strictly left to right.
\end{conceptbox}

\vspace{4pt}
\begin{conceptbox}[aiGreen]{Knowledge Distillation and DistilBERT}
DistilBERT~\cite{distilbert} is obtained through \textbf{knowledge distillation}: a smaller \textit{student} model is trained to replicate the output distribution of a larger \textit{teacher} model (BERT), rather than learning from raw labels alone. The result retains approximately 97\,\% of BERT's performance on downstream tasks while using 40\,\% fewer parameters and running 60\,\% faster.
\end{conceptbox}

The variant used in this project was fine-tuned on the \textbf{Stanford Sentiment Treebank} (SST-2)~\cite{sst2}, a benchmark dataset of sentence-level sentiment annotations drawn from movie reviews. Its classification of short evaluative sentences transfers naturally to marketplace reviews. The model returns a binary label (positive or negative) alongside a confidence score; reviews with a confidence below 0.65 are mapped to the \textit{neutral} class to avoid committing to a label when the signal is ambiguous.

\paragraph{Fallback Model --- Logistic Regression.}
A secondary \textbf{Logistic Regression}~\cite{sklearn} classifier trained on TF-IDF features serves as an offline fallback when the cloud inference API is unavailable. Logistic Regression estimates the probability of class membership by applying the logistic function to a linear combination of input features. It produces a three-class output (positive, neutral, negative) and is well suited to this role because of its low computational footprint and interpretable decision boundaries. Table~\ref{tab:sentiment_matrix} summarizes the runtime decision logic.

\begin{table}[H]
\centering
\caption{Sentiment classification --- decision logic}
\label{tab:sentiment_matrix}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|L{5.2cm}|L{3.5cm}|L{4.3cm}|}
\hline
\rowcolor{isiBlue}
\textcolor{white}{\textbf{Condition}} &
\textcolor{white}{\textbf{Model}} &
\textcolor{white}{\textbf{Output label}} \\
\hline
Cloud API available, confidence $\geq$ 0.65 & DistilBERT  & positive or negative \\
\hline
Cloud API available, confidence $<$ 0.65    & DistilBERT  & neutral \\
\hline
Cloud API unavailable                        & Logistic Regression & positive, neutral or negative \\
\hline
\end{tabular}
\end{table}

\subsubsection{Realization}

Each review is analyzed immediately upon submission. The resulting sentiment label is stored alongside the review and displayed as a color-coded badge on the asset detail page. The seller dashboard aggregates all labels into a summary chart visible from the Creator Studio.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{images/realisation/marketplace/ai reviews.png}
\caption{Sentiment analysis --- color-coded sentiment badges displayed on the asset detail page}
\label{fig:sentiment_marketplace}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{images/realisation/workspace/ai reviews (resume).png}
\caption{Sentiment analysis --- aggregate sentiment summary visible in the Workspace admin view}
\label{fig:sentiment_interface}
\end{figure}

\subsubsection{Conclusion}
Automated sentiment classification reduces the effort required to audit large volumes of reviews. Moderators can quickly identify trends and sellers receive actionable feedback without reading every individual entry.

% ── 5.2.3 PERSONALIZED RECOMMENDATIONS ───────────────────────────────────────
The third AI service surfaces relevant assets to each user based on their interaction history.

\subsection{Personalized Recommendations}

\subsubsection{Analysis}

\paragraph{Overview.}
The recommendation system surfaces relevant assets to each user based on their interaction history. It follows a \textbf{content-based filtering} approach~\cite{cbf}, which derives user preferences from past behaviour and matches them against asset attributes rather than comparing users with one another. This design does not require a large user base to produce meaningful results, making it well-suited to a growing marketplace.

\paragraph{Similarity Measure --- Jaccard Index.}
Each asset is represented by its set of descriptive tags.

\vspace{4pt}
\begin{conceptbox}[aiOrange]{Jaccard Similarity Index}
The \textbf{Jaccard similarity index}~\cite{jaccard} quantifies the overlap between two sets $A$ and $B$ as the ratio of their intersection to their union:
\[
J(A,\,B) \;=\; \frac{|A \cap B|}{|A \cup B|}
\]
A value of 1 indicates perfectly identical sets; a value of 0 indicates no overlap at all. The Jaccard index is well suited to recommendation tasks based on tags because the attributes are binary (present or absent) and the sets vary widely in size across categories.
\end{conceptbox}

\paragraph{User Profile Construction.}
The user's preference profile is built from four interaction types, each weighted according to the strength of preference it expresses:

\begin{table}[H]
\centering
\caption{User interaction signals and their weights}
\label{tab:rec_signals}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|L{4.5cm}|C{2cm}|L{6.5cm}|}
\hline
\rowcolor{isiBlue}
\textcolor{white}{\textbf{Interaction}} &
\textcolor{white}{\textbf{Weight}} &
\textcolor{white}{\textbf{Rationale}} \\
\hline
Asset like         & 5 & Strongest explicit preference signal \\
\hline
Paid purchase      & 4 & High-confidence revealed preference \\
\hline
Asset download     & 3 & Direct acquisition of content \\
\hline
Star rating        & 1--5 & Weighted proportionally to the numeric value \\
\hline
\end{tabular}
\end{table}

\paragraph{Diversity and Cold-Start Handling.}
To prevent the ranked list from being dominated by near-identical assets, a post-processing diversity pass demotes any item whose tag overlap with a higher-ranked result exceeds 70\,\%. Users with no recorded interactions — guests and newly registered accounts — receive a trending list ranked by recent download and purchase frequency, and the interface displays ``Trending Now'' rather than ``Recommended for You'' in this cold-start state.

\begin{figure}[H]
\centering
\includegraphics[width=0.62\textwidth]{images/description/ai recommendations.png}
\caption{AI Recommendations Pipeline --- warm path (personalized) and cold-start path (trending)}
\label{fig:recommendations_pipeline}
\end{figure}

\subsubsection{Realization}

The recommendation page is accessible to both authenticated and anonymous users. Users with sufficient interaction history receive a personalized ranking; all others see the trending catalog. The interface is visually consistent with the standard catalog pages.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{images/realisation/marketplace/recommandations-ai.png}
\caption{Recommendations page --- personalized (warm user) and trending (cold start)}
\label{fig:recommendations_interface}
\end{figure}

\subsubsection{Conclusion}
Content-based filtering with Jaccard similarity delivers personalized results without requiring collaborative data or model retraining. The cold-start strategy ensures every visitor receives meaningful content from the first interaction.

Alongside the AI services, three independent security mechanisms were implemented to protect the platform against common attack vectors.

% ============================================================
% SECURITY HARDENING
% ============================================================
\section{Security Hardening}

Three independent security mechanisms protect the platform against the most common attack vectors catalogued in the OWASP Top Ten~\cite{owasp}.

\begin{itemize}[leftmargin=2em, itemsep=6pt]
  \item \textbf{Rate limiting.}
        Each sensitive endpoint is protected by a per-IP request ceiling. Authentication
        attempts are limited to ten requests per fifteen-minute window, and account
        registration is capped at five per hour. This mitigates brute-force and
        credential-stuffing attacks by making exhaustive enumeration computationally
        impractical.

  \item \textbf{Token revocation.}
        Authentication tokens are immediately invalidated upon logout through a
        server-side blacklist consulted on every authenticated request. This ensures
        that a token intercepted after the user has signed out cannot be reused,
        regardless of its expiry date.

  \item \textbf{Magic-byte file validation.}
        Uploaded images are validated at the binary level against the known
        \textbf{magic bytes} of accepted formats (PNG, JPEG, WebP, GIF). Magic bytes
        are fixed byte sequences at the start of a file that identify its true format
        independently of the filename or MIME type declared by the client. A file
        whose binary header does not match its declared type is rejected before
        reaching storage, preventing MIME-confusion attacks in which a malicious file
        is disguised as a safe image.
\end{itemize}



Beyond the mechanisms listed above, the OWASP coverage also includes: broken access control addressed through the RBAC permission model, injection risks eliminated through parameterized database queries throughout the data layer, and security misconfiguration mitigated by security-oriented HTTP headers and mandatory environment-variable validation at application startup.

Beyond security and AI capabilities, the platform offers an immersive preview experience enabled by the WebXR standard.

% ============================================================
% VR/AR IMMERSIVE PREVIEW
% ============================================================
\section{VR/AR Immersive Preview}

This section examines the technical foundations of the immersive preview and presents the validated implementation.

\subsection{Analysis}

\paragraph{Browser-Native Immersive Web.}
The immersive preview feature is delivered entirely through the browser, with no plugin or companion application required on the user's side. This is made possible by a recent W3C standard that exposes virtual and augmented reality hardware directly to web pages.

\vspace{4pt}
\begin{conceptbox}[aiPurple]{WebXR Device API}
The \textbf{WebXR Device API}~\cite{webxr} is a W3C standard that provides browsers with a unified interface for accessing virtual and augmented reality hardware. It abstracts the differences between headset models, controller layouts and spatial tracking systems, allowing a single web application to deliver immersive experiences across a variety of XR devices without platform-specific adaptations.
\end{conceptbox}

Because the API is implemented natively in modern browsers, the immersive preview is
accessible to anyone owning a compatible headset, without any installation step.

\paragraph{3D Rendering and Stereo Mode.}\leavevmode

\vspace{4pt}
\begin{conceptbox}[aiPurple]{WebGL --- Web Graphics Library}
\textbf{WebGL}~\cite{webgl} is a JavaScript API standardized by the Khronos Group that provides direct access to the device's GPU from within a web browser, without plugins. It exposes a subset of the OpenGL ES 2.0 API, enabling the browser to execute shader programs on the GPU and render complex 3D scenes at interactive frame rates. Because WebGL runs inside the browser's security sandbox, it achieves near-native rendering performance while remaining safe for untrusted web content.
\end{conceptbox}

The asset viewer is powered by \textbf{Three.js}~\cite{threejs}, a JavaScript library
that abstracts the low-level WebGL rendering pipeline. When a user enters immersive
mode, the renderer transitions to \textbf{stereoscopic projection}, producing a
separate image frame for each eye at the correct inter-pupillary offset to create a
convincing perception of depth. In augmented reality mode, the rendered 3D asset is
composited over the live camera feed provided by the headset's passthrough cameras,
allowing the user to inspect the object within their physical environment.

To maintain rendering correctness in XR sessions, visual post-processing effects
that rely on multi-pass framebuffer operations are deactivated during immersive
rendering. These effects are not designed for the stereo framebuffer required by
headsets and would otherwise cause visual artefacts or rendering errors.

\paragraph{Hardware Validation.}
The feature was validated on a \textbf{Meta Quest 3}~\cite{metaquest} mixed-reality
headset. The device was connected to the same local network as the development server
and accessed the application through its built-in browser over HTTPS, which is
required by the WebXR specification. Two modes were tested: a fully immersive
\textbf{VR mode} in which the user is surrounded by the rendered 3D scene, and a
\textbf{passthrough AR mode} in which the asset is overlaid on the real environment
captured by the device's colour cameras. Both modes operated correctly, confirming
that the platform delivers an authentic WebXR experience on consumer mixed-reality
hardware.

The following subsection demonstrates the immersive preview experience validated on consumer mixed-reality hardware.

\subsection{Realization}

On any compatible 3D asset detail page, the asset is rendered interactively in the
browser using a Three.js scene with orbit controls, post-processing effects, and
environment lighting. The viewer supports \texttt{.glb} and \texttt{.gltf} formats
and allows buyers to examine geometry, materials, and surface detail at any angle
before purchasing.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{images/realisation/marketplace/3d model.png}
\caption{3D model viewer --- interactive orbit controls and environment lighting in the browser}
\label{fig:model_viewer}
\end{figure}

From the same page, the user can enter VR or AR mode directly from the browser with a
single click. In VR mode, the asset is rendered at true real-world scale inside the
headset, allowing buyers to evaluate proportions and surface detail. In AR mode, the
asset is placed within the user's physical environment using the headset's passthrough
cameras.

\begin{figure}[H]
\centering
\begin{minipage}[t]{0.48\textwidth}
  \centering
  \includegraphics[width=\textwidth]{images/realisation/vr preview 1.jpg}
  \caption{3D asset rendered at full scale on Meta Quest 3 - 1}
  \label{fig:vr_preview_1}
\end{minipage}
\hfill
\begin{minipage}[t]{0.48\textwidth}
  \centering
  \includegraphics[width=\textwidth]{images/realisation/vr preview 2.jpg}
  \caption{3D asset rendered at full scale on Meta Quest 3 - 2}
  \label{fig:vr_preview_2}
\end{minipage}
\end{figure}

Once all features were validated, the platform was deployed to a production environment, as described in the following section.

% ============================================================
% DEPLOYMENT
% ============================================================
\section{Deployment on Hostinger VPS}

This section describes the production infrastructure, server configuration and the automated delivery pipeline.

\subsection{Infrastructure Overview}

The production environment is organized around three independent tiers, each handling a
distinct concern. The compute tier consists of a \textbf{Hostinger Virtual Private Server}
running Ubuntu~22.04 LTS, which hosts the Node.js backend and serves the two compiled
React applications as static files. The data tier relies on \textbf{Supabase}, a
cloud-hosted service that manages both the PostgreSQL database and the private file
storage buckets, eliminating the need to maintain a database instance on the server
itself. Finally, the AI tier delegates all inference workloads to the
\textbf{Hugging Face Inference API}, meaning no GPU hardware is required on the VPS.

The server configuration details how incoming traffic is handled, how HTTPS is enforced and how the application process is kept alive.

\subsection{Server Configuration}

\paragraph{Reverse Proxy and HTTPS.}
\textbf{Nginx} acts as the single entry point for all incoming traffic. It terminates
HTTPS connections using a free TLS certificate issued by \textbf{Let's Encrypt} and
renewed automatically every ninety days. Secure connections are not optional: the
WebXR specification mandates HTTPS before a browser will grant access to XR hardware.
Nginx routes API and real-time traffic to the backend process while serving the
frontend builds directly from disk.

\vspace{4pt}
\begin{conceptbox}[aiGreen]{PM2 — Node.js Process Manager}
\textbf{PM2} is a production-grade process manager for Node.js applications. It keeps
the server process alive after crashes, restarts it automatically on reboot, and enables
zero-downtime reloads so that new deployments do not interrupt active user sessions.
Log rotation and resource monitoring are handled natively.
\end{conceptbox}

\paragraph{Secrets Management.}
All sensitive configuration — database credentials, JWT signing keys, payment gateway
tokens, and third-party API keys — is stored in environment variables on the server and
never committed to the source repository. This ensures that the Git history remains free
of credentials and that different environments (development, production) can carry their
own configuration without code changes.

The final element of the deployment setup is the automated continuous integration and delivery pipeline.

\subsection{CI/CD Pipeline}

\textbf{Continuous Integration (CI)} is the practice of automatically building and testing every code change as soon as it is pushed to the shared repository. The goal is to detect integration problems immediately, before they accumulate into larger conflicts. \textbf{Continuous Delivery (CD)} extends CI by automatically preparing and deploying a validated build to the target environment, so that every passing build can be released to users without manual intervention.

\paragraph{Automated Delivery with GitHub Actions.}
The project uses \textbf{GitHub} for version control and \textbf{GitHub Actions} as the
CI/CD platform. A workflow triggered on every merge to the main branch performs three sequential stages: the \emph{CI stage} installs dependencies and executes the integration test suite to confirm the build is healthy; the \emph{packaging stage} compiles both React frontends into optimized static bundles; and the \emph{CD stage} connects to the VPS over SSH to pull the latest revision, deploy the new bundles, and reload the backend process with zero downtime. Deployment credentials are stored as encrypted repository secrets and are never exposed in workflow logs.

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{images/description/github_actions.png}
\caption{GitHub Actions CI/CD pipeline --- automated build, test, and deployment workflow}
\label{fig:deployment_arch}
\end{figure}

% ============================================================
% CHAPTER 5 CONCLUSION
% ============================================================
\section*{Conclusion}
This chapter completed the platform's feature set by delivering the AI services, immersive previews, and production infrastructure that distinguish the IGS Marketplace from conventional asset stores. The AI pipeline --- combining intent classification, sentiment analysis, and content-based recommendations --- adds intelligence to the user experience without requiring external collaborative data. With the VPS deployment and CI/CD pipeline in place, the platform is ready for production. A general conclusion summarizing the project's achievements and future perspectives follows.
