% Class Diagram — Sprint 1 Core Domain (User, Role, Permission, AuditLog)
\begin{tikzpicture}[
  cls/.style={rectangle, draw=isiBlue, fill=blue!5, minimum width=4.2cm,
              align=left, font=\ttfamily\footnotesize},
  hdr/.style={rectangle, draw=isiBlue, fill=isiBlue, minimum width=4.2cm,
              align=center, font=\small\bfseries\color{white}},
  arr/.style={->, thick, draw=darkGray},
  comp/.style={-{Diamond}, thick, draw=darkGray}]

% ── User class ──────────────────────────────────────────────────────
\node[hdr] (uh) at (0, 8)    {User};
\node[cls, anchor=north, text width=4.1cm] (ub) at (0, 7.65) {
  \textbf{-- id:} UUID\\
  \textbf{-- supabase\_uid:} String\\
  \textbf{-- email:} String\\
  \textbf{-- username:} String\\
  \textbf{-- user\_type:} Enum\\
  \textbf{-- is\_active:} Boolean\\
  \textbf{-- created\_at:} Date\\
  \rule{3.9cm}{0.3pt}\\
  \textbf{+ getProfile(): UserDTO}\\
  \textbf{+ updateProfile(): void}
};

% ── Role class ───────────────────────────────────────────────────────
\node[hdr] at (6.5, 8) {Role};
\node[cls, anchor=north, text width=4.1cm] (rb) at (6.5, 7.65) {
  \textbf{-- id:} Integer\\
  \textbf{-- name:} String\\
  \textbf{-- level:} Integer\\
  \textbf{-- user\_type:} Enum\\
  \rule{3.9cm}{0.3pt}\\
  \textbf{+ hasPermission(p): Boolean}
};

% ── Permission class ─────────────────────────────────────────────────
\node[hdr] at (6.5, 4.5) {Permission};
\node[cls, anchor=north, text width=4.1cm] (pb) at (6.5, 4.15) {
  \textbf{-- id:} Integer\\
  \textbf{-- name:} String\\
  \textbf{-- description:} String\\
  \rule{3.9cm}{0.3pt}\\
  \textbf{+ check(role): Boolean}
};

% ── AuditLog class ───────────────────────────────────────────────────
\node[hdr] at (0, 3.5) {AuditLog};
\node[cls, anchor=north, text width=4.1cm] (ab) at (0, 3.15) {
  \textbf{-- id:} UUID\\
  \textbf{-- user\_id:} UUID\\
  \textbf{-- action:} String\\
  \textbf{-- entity:} String\\
  \textbf{-- entity\_id:} UUID\\
  \textbf{-- metadata:} JSONB\\
  \textbf{-- created\_at:} Date\\
  \rule{3.9cm}{0.3pt}\\
  \textbf{+ log(user, action): void}
};

% ── Associations ─────────────────────────────────────────────────────
% User has one Role (1..1)
\draw[arr] (2.1, 7.3) -- node[above, font=\tiny] {has} node[below, font=\tiny] {1\quad\quad 1..*} (4.4, 7.3);

% Role has many Permissions (many-to-many via role_permissions)
\draw[arr] (6.5, 5.85) -- node[right, font=\tiny] {grants} node[left, font=\tiny] {\quad\quad\quad 0..*} (6.5, 4.55);

% User produces AuditLog (1..*)
\draw[arr] (0, 5.8) -- node[left, font=\tiny] {produces} node[right, font=\tiny] {1\quad\quad\;0..*} (0, 3.8);

% Association label for role-permissions junction
\node[draw=darkGray!50, fill=white, font=\tiny\itshape, rounded corners=2pt,
      align=center, text width=2.4cm]
  at (7.9, 6.7) {<<table>>\\[1pt]role\_permissions};

\end{tikzpicture}
