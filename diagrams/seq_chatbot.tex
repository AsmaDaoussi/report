% ============================================================
% Sequence Diagram - AI Chatbot Pipeline (Visual Paradigm style)
% User -> BotWidget -> /api/bot -> intentService -> assetRepo -> HF Qwen 2.5
%   alt [intent = product_search] -> tool layer
%   alt [HF unavailable]          -> rule-based fallback
% ============================================================
\begin{tikzpicture}[
  font=\sffamily\scriptsize,
  part/.style={rectangle, draw=darkGray, line width=0.6pt,
               fill=yellow!18, text=black,
               minimum width=2.5cm, minimum height=0.7cm,
               font=\sffamily\footnotesize\bfseries,
               align=center, inner sep=3pt},
  ll/.style={dashed, draw=darkGray!70, line width=0.45pt},
  activ/.style={rectangle, draw=darkGray, line width=0.5pt, fill=isiBlue!12},
  sync/.style ={-{Stealth[length=2.2mm]}, draw=darkGray, line width=0.7pt},
  ret/.style  ={-{Latex[length=2mm]},     draw=darkGray, line width=0.55pt, dashed},
  note/.style={rectangle, draw=orange!70!black, line width=0.5pt,
               fill=yellow!30, font=\scriptsize, align=left,
               inner sep=3pt, rounded corners=1pt},
  frame/.style={rectangle, dashed, draw=darkGray, line width=0.55pt},
  flabel/.style={rectangle, draw=darkGray, line width=0.55pt,
                 fill=white, font=\scriptsize\bfseries,
                 inner sep=2pt, anchor=north west},
  lbl/.style={font=\scriptsize, fill=white, inner sep=1pt}
]

  % ---- Participants ----
  \node[part] (user)    at (0,    0)  {User};
  \node[part] (widget)  at (3.0,  0)  {BotWidget};
  \node[part] (bot)     at (6.0,  0)  {/api/bot};
  \node[part] (intent)  at (9.0,  0)  {intent\\Service};
  \node[part] (db)      at (12.0, 0)  {assetRepo};
  \node[part] (hf)      at (15.0, 0)  {HF Qwen\\2.5-72B};

  % ---- Lifelines ----
  \foreach \x in {0, 3.0, 6.0, 9.0, 12.0, 15.0}{
    \draw[ll] (\x,-0.4) -- (\x,-11.5);
  }

  % ---- Activation boxes ----
  \filldraw[activ] (-0.12,-0.6)   rectangle (0.12,-10.0);
  \filldraw[activ] (2.88,-1.0)    rectangle (3.12, -9.5);
  \filldraw[activ] (5.88,-1.6)    rectangle (6.12, -9.0);
  \filldraw[activ] (8.88,-2.2)    rectangle (9.12, -3.4);
  \filldraw[activ] (11.88,-4.0)   rectangle (12.12,-5.0);
  \filldraw[activ] (14.88,-6.4)   rectangle (15.12,-7.4);

  % ---- 1: User types ----
  \draw[sync] (0.12,-1.0) -- node[lbl] {1: type message} (2.88,-1.0);

  % ---- 2: POST to bot ----
  \draw[sync] (3.12,-1.6) -- node[lbl] {2: POST /api/bot/message \{text\}} (5.88,-1.6);

  % ---- 3: Classify intent ----
  \draw[sync] (6.12,-2.2) -- node[lbl] {3: classify(text)} (8.88,-2.2);
  \draw[ret]  (8.88,-3.2) -- node[lbl] {intent, confidence} (6.12,-3.2);

  % ---- alt [intent = product_search] ----
  \draw[frame] (5.3,-3.6) rectangle (13.0,-5.4);
  \node[flabel] at (5.3,-3.6) {alt [intent = product\_search]};

  \draw[sync] (6.12,-4.0) -- node[lbl] {4: searchAssets(keywords)} (11.88,-4.0);
  \draw[ret]  (11.88,-4.9) -- node[lbl] {top 5 results} (6.12,-4.9);

  % ---- Build LLM context ----
  \node[note, anchor=north west] at (6.25,-5.6)
       {\textbf{5:} build LLM context\\
        system prompt + history + (optional) results};

  % ---- 6: Invoke LLM ----
  \draw[sync] (6.12,-6.4) -- node[lbl] {6: chatCompletion(Qwen2.5-72B)} (14.88,-6.4);
  \draw[ret]  (14.88,-7.3) -- node[lbl] {assistant message} (6.12,-7.3);

  % ---- alt [HF unavailable] ----
  \draw[frame] (5.3,-7.7) rectangle (13.0,-8.7);
  \node[flabel] at (5.3,-7.7) {alt [HF unavailable]};
  \node[lbl, anchor=west] at (5.5,-8.3) {fallback to intent-keyed rule-based template};

  % ---- 7: Return reply ----
  \draw[ret] (5.88,-9.0) -- node[lbl] {7: 200 \{ reply, intent, sources \}} (3.12,-9.0);
  \draw[ret] (2.88,-9.5) -- node[lbl] {8: render bubble} (0.12,-9.5);

  % ---- Termination ----
  \foreach \x in {0, 3.0, 6.0, 9.0, 12.0, 15.0}{
    \draw[line width=0.8pt] (\x-0.15,-10.05) -- (\x+0.15,-10.05);
  }

\end{tikzpicture}
