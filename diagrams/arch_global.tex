% Global Architecture Diagram -- Two Apps, One Backend
\begin{tikzpicture}[
  app/.style={rectangle, draw=isiBlue, fill=blue!8, rounded corners=4pt,
              minimum width=3.2cm, minimum height=1.2cm, align=center,
              font=\small\bfseries, text=isiBlue},
  service/.style={rectangle, draw=igsGreen, fill=green!6, rounded corners=4pt,
                  minimum width=3.2cm, minimum height=1cm, align=center,
                  font=\small, text=black},
  backend/.style={rectangle, draw=isiBlue, fill=isiBlue, rounded corners=4pt,
                  minimum width=4cm, minimum height=1.4cm, align=center,
                  font=\small\bfseries, text=white},
  db/.style={cylinder, draw=darkGray, fill=gray!10, shape border rotate=90,
             minimum width=2.5cm, minimum height=1cm, align=center,
             font=\footnotesize\bfseries},
  arr/.style={->, thick, draw=isiBlue},
  arr2/.style={->, thick, draw=igsGreen, dashed},
  lbl/.style={font=\tiny\itshape, midway}]

  % === Frontend Layer ===
  \node[font=\small\bfseries\color{darkGray}] at (0, 5.8) {Frontend Layer};
  \node[app] (marketplace) at (-2.5, 5) {Marketplace\\React + Vite\\:5173};
  \node[app] (workspace) at (2.5, 5) {IGS Workspace\\React + Vite\\:5174};

  % Shared token annotation
  \draw[<->, draw=orange, thick, dashed] (marketplace) -- node[above, font=\tiny\color{orange}] {Shared JWT (localStorage)} (workspace);

  % === Backend Layer ===
  \node[font=\small\bfseries\color{darkGray}] at (0, 3.2) {Backend Layer};
  \node[backend] (api) at (0, 2.6) {Express.js REST API\\Node.js -- :3001};

  % Frontend → Backend arrows
  \draw[arr] (marketplace.south) -- node[lbl, left] {REST} ++(0,-0.6) -| (api.north);
  \draw[arr] (workspace.south)   -- node[lbl, right] {REST + WS} ++(0,-0.6) -| (api.north);

  % === Services Layer ===
  \node[font=\small\bfseries\color{darkGray}] at (0, 0.8) {Services \& Data Layer};

  \node[db] (pg) at (-4.5, 0) {PostgreSQL\\Database};
  \node[service] (supabase) at (-1.5, 0) {Supabase\\Storage};
  \node[service] (firebase) at (1.5, 0) {Firebase\\Auth};
  \node[service] (stripe) at (4.5, 0) {Stripe\\Payment};

  % Backend → Services
  \draw[arr] (api.south) -| node[lbl, near end, left] {SQL} (pg.north);
  \draw[arr2] (api.south) -| node[lbl, near end, left] {Files} (supabase.north);
  \draw[arr2] (api.south) -| node[lbl, near end, right] {Auth} (firebase.north);
  \draw[arr2] (api.south) -| node[lbl, near end, right] {Pay} (stripe.north);

  % Socket.io annotation
  \node[draw=orange, fill=orange!10, rounded corners=3pt, font=\tiny,
        minimum width=2cm, minimum height=0.5cm, align=center] (sio) at (0, -1.2)
        {Socket.io (Real-time Chat)};
  \draw[->, draw=orange, dashed] (api.south) -- (sio.north);

\end{tikzpicture}
